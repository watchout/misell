schema_version: shirube-audit-checklist/v1
audit_checklist_id: AUDIT-CHECKLIST-MISELL-211-STUDIO-PROVIDER-STATUS-PANEL
source:
  handoff_ref: .shirube/control-handoffs/CH-MISELL-211-studio-provider-status-panel.yaml
  cell_ref: .shirube/cells/CELL-MISELL-211-STUDIO-PROVIDER-STATUS-PANEL.yaml
  cell_id: CELL-MISELL-211-STUDIO-PROVIDER-STATUS-PANEL
  pr: watchout/misell#225
items:
  - item_id: SP-STATUS-AUDIT-001
    source: allowed_paths
    verification_method: executable
    required: true
    prompt: Verify all changed files are inside handoff allowed_paths and no forbidden server/package/workflow/player/deploy/secret paths are touched.
  - item_id: SP-STATUS-AUDIT-002
    source: read_only_boundary
    verification_method: semantic
    required: true
    prompt: Verify the panel uses only existing #211 read APIs and does not add create/start/complete/fail/delete/provenance approval controls.
  - item_id: SP-STATUS-AUDIT-003
    source: side_effect_boundary
    verification_method: semantic
    required: true
    prompt: Verify UI copy and behavior preserve no external provider call, no secret exposure, no credit consumption, no content_manifest creation, no publish, and no media generation.
  - item_id: SP-STATUS-AUDIT-004
    source: scope
    verification_method: semantic
    required: true
    prompt: Verify jobs and provenance are fetched with campaign_project_id scope and the provider catalog is displayed as active catalog metadata only.
  - item_id: SP-STATUS-AUDIT-005
    source: e2e
    verification_method: executable
    required: true
    prompt: Verify E2E covers provider status panel visibility, empty-state handling, provider catalog display, and absence of mutation controls.
  - item_id: SP-STATUS-AUDIT-006
    source: validation
    verification_method: executable
    required: true
    prompt: Verify required validation commands passed and exact head is recorded.
